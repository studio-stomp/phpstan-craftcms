version: 2
updates:
    # Enable version updates for composer
    -   package-ecosystem: "composer"
        # Look for `composer.json` and `lock` files in the `root` directory
        directory: "/"
        # Check the packagist registry for updates every day (weekdays)
        schedule:
            interval: "daily"
